# Future Steps

1. **Path Aliases**: Update `tsconfig.json` to use `@/*` for `src/`.
2. **UI Integration**: Connect Frontend components (Dashboard, Nourish) to Backend `/api/analyze` and `/api/recipe` endpoints using tRPC or Fetch.
3. **Cloud Run Deployment**: Push `Dockerfile` to Artifact Registry and deploy.
4. **State Management**: Integrate `Zustand` for biometric and meal state tracking across routes.
5. **Interactive Adaptation**: Implement "Start Adaptation" modal logic in Nourish page.
